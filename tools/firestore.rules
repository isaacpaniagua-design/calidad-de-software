    // Permitir ambos nombres: snake_case y camelCase para compatibilidad frontend
    match /studentUploads/{uploadId} {
      allow get: if isTeacher() || (isAuthenticated() && request.auth.uid == resource.data.student.uid);

      allow list: if isTeacher() || (isAuthenticated() && request.query.where[0][0] == 'student.uid' && request.query.where[0][2] == request.auth.uid);

      allow create: if isTeacher() || (isAuthenticated() && request.auth.uid == request.resource.data.student.uid);
      
      allow update, delete: if isTeacher();
    }
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    function isAuthenticated() {
      return request.auth != null;
    }

    // ⭐️ REGLA CORREGIDA Y ROBUSTA PARA DOCENTES
    // Un usuario es docente si tiene el custom claim `isTeacher` O si su UID
    // existe como documento en la colección /teachers. Esto cubre ambos escenarios.
    function isTeacher() {
      return isAuthenticated() && 
             (request.auth.token.isTeacher == true || exists(/databases/$(database)/documents/teachers/$(request.auth.uid)));
    }

    match /students/{studentId} {
      allow read, write: if isTeacher();
    }

    match /grades/{studentId} {
      // Docente puede leer y escribir todas las calificaciones
      allow read, write: if isTeacher();
      // Estudiante solo puede leer su propio documento
      allow read: if isAuthenticated() && resource.data.authUid == request.auth.uid;

      match /activities/{activityId} {
        // Docente puede leer y escribir todas las actividades
        allow read, write: if isTeacher();
        // Estudiante solo puede leer sus propias actividades
        allow read: if isAuthenticated() && request.auth.uid == studentId;
      }
    }

    match /activities/{activityId} {
      allow read: if isAuthenticated();
      allow write: if isTeacher();
    }

    match /teachers/{teacherId} {
      allow read: if isTeacher();
      allow write: if false; // Los docentes no se pueden modificar a sí mismos desde el cliente.
    }
    
    match /student_uploads/{uploadId} {
      allow get: if isTeacher() || (isAuthenticated() && request.auth.uid == resource.data.student.uid);

      allow list: if isTeacher() || (isAuthenticated() && request.query.where[0][0] == 'student.uid' && request.query.where[0][2] == request.auth.uid);

      allow create: if isTeacher() || (isAuthenticated() && request.auth.uid == request.resource.data.student.uid);
      
      allow update, delete: if isTeacher();
    }
    
    match /materials/{materialId} {
      allow read: if isAuthenticated();
      allow write: if isTeacher();
    }
    match /forum_topics/{topicId} {
      allow read, write: if isAuthenticated();
      match /replies/{replyId} {
        allow read, write: if isAuthenticated();
      }
    }

    match /attendances/{attendanceId} {
      allow read: if isAuthenticated();
      allow create: if isAuthenticated() &&
                       request.auth.uid == request.resource.data.uid &&
                       request.auth.uid == request.resource.data.createdByUid;
      allow update, delete: if isTeacher();
    }

    match /planesDePrueba/{planId} {
      allow read, write: if isTeacher();
    }
    match /courses/{courseId} {
      allow read: if isAuthenticated();
      allow write: if isTeacher();
    }
    match /grupos/{grupoId} {
      allow read: if isAuthenticated();
      allow write: if isTeacher();
      match /members/{memberId} {
        allow read, write: if isTeacher();
      }
    }
  }
}